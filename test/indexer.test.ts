import { describe, expect, it } from 'vitest';
import * as fs from 'node:fs'; import * as os from 'node:os'; import * as path from 'node:path';
import { discoverFiles, indexRepository, isIgnored } from '../src/intelligence/indexer.js';
function fixture(){const root=fs.mkdtempSync(path.join(os.tmpdir(),'lgs-index-'));fs.mkdirSync(path.join(root,'src'));fs.mkdirSync(path.join(root,'node_modules'));fs.writeFileSync(path.join(root,'src','b.ts'),'export const value = 1;');fs.writeFileSync(path.join(root,'src','a.ts'),'import { value } from "./b.js"; export function run(){ return value; }');fs.writeFileSync(path.join(root,'README.md'),'# Fixture');fs.writeFileSync(path.join(root,'node_modules','ignored.js'),'ignored');return root;}
describe('repository indexer',()=>{it('discovers files and ignores generated/dependency directories',()=>{const root=fixture();expect(discoverFiles(root)).toEqual(['README.md','src/a.ts','src/b.ts']);expect(isIgnored('node_modules/a.js')).toBe(true);expect(isIgnored('src/a.ts')).toBe(false);fs.rmSync(root,{recursive:true,force:true});});it('extracts symbols, imports, exports, and documentation markers',()=>{const root=fixture();const index=indexRepository(root);const file=index.files.find(x=>x.path==='src/a.ts')!;expect(file.imports).toContain('./b.js');expect(file.exports).toContain('run');expect(file.symbols).toContain('run');expect(index.files.find(x=>x.path==='README.md')?.likelyDocumentation).toBe(true);expect(index.moduleRelationships).toContainEqual({from:'src/a.ts',to:'src/b.ts'});fs.rmSync(root,{recursive:true,force:true});});it('reuses unchanged files and detects changes, additions, removals, and renames',()=>{const root=fixture();const first=indexRepository(root);fs.writeFileSync(path.join(root,'src','a.ts'),'export const changed = true;');fs.renameSync(path.join(root,'src','b.ts'),path.join(root,'src','renamed.ts'));fs.writeFileSync(path.join(root,'src','new.ts'),'export const newer = true;');const second=indexRepository(root,first);expect(second.incremental.reused).toContain('README.md');expect(second.incremental.changed).toContain('src/a.ts');expect(second.incremental.added).toContain('src/new.ts');expect(second.incremental.renamed).toContainEqual({from:'src/b.ts',to:'src/renamed.ts'});fs.rmSync(root,{recursive:true,force:true});});it('does not fail on malformed source',()=>{const root=fixture();fs.writeFileSync(path.join(root,'src','broken.ts'),'export function {');expect(()=>indexRepository(root)).not.toThrow();expect(indexRepository(root).files.find(x=>x.path==='src/broken.ts')).toBeDefined();fs.rmSync(root,{recursive:true,force:true});});});
