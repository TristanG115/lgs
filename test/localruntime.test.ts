import { describe, expect, it } from 'vitest'; import * as fs from 'node:fs'; import * as os from 'node:os'; import * as path from 'node:path'; import { BenchmarkStore, metrics } from '../src/tools/index.js';
describe('local runtime discovery and benchmarks',()=>{it('persists benchmark speed separately from quality',()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'lgs-runtime-'));const store=new BenchmarkStore(root);const value=store.append({runtimeId:'ollama:http://localhost',model:'local',case:'planning',metrics:{totalLatencyMs:100,outputTokens:20,tokensPerSecond:200},quality:{score:75,notes:'Follows instructions.'}});expect(store.read()).toMatchObject([{id:value.id,quality:{score:75},metrics:{tokensPerSecond:200}}]);expect(metrics(0,{inputTokens:50,outputTokens:20},100).contextUtilization).toBe(0.5);fs.rmSync(root,{recursive:true,force:true});});});
