## LGS frontend

When changing any visible extension UI, webview, chat experience, provider
settings UI, activity presentation, or frontend architecture, use the
`lgs-frontend` skill.

The rendered VS Code extension is the source of truth for frontend quality.
Functional correctness alone is not sufficient.