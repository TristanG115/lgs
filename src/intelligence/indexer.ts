import * as fs from 'node:fs';
import * as path from 'node:path';
import { createHash } from 'node:crypto';
import * as ts from 'typescript';
export type IndexedFile={path:string;fileType:string;language:string;size:number;mtimeMs:number;hash:string;imports:string[];exports:string[];symbols:string[];likelyTest:boolean;likelyDocumentation:boolean};
export type RepositoryIndex={version:1;generatedAt:string;root:string;directories:string[];files:IndexedFile[];moduleRelationships:{from:string;to:string}[];manifests:string[];dependencies:Record<string,string>;incremental:{reused:string[];added:string[];changed:string[];removed:string[];renamed:{from:string;to:string}[]}};
const IGNORED=new Set(['.git','node_modules','dist','out','build','coverage','.cache','.turbo','.lgs']);const SOURCE=new Set(['.ts','.tsx','.js','.jsx','.mjs','.cjs']);const DOCS=new Set(['.md','.mdx','.txt','.rst']);const MANIFESTS=new Set(['package.json','tsconfig.json','jsconfig.json','pnpm-lock.yaml','yarn.lock','package-lock.json']);
export function discoverFiles(root:string):string[]{const result:string[]=[];function visit(dir:string){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){if(entry.isDirectory()&&IGNORED.has(entry.name))continue;const absolute=path.join(dir,entry.name);if(entry.isDirectory())visit(absolute);else if(entry.isFile())result.push(relative(root,absolute));}}visit(root);return result.sort();}
export function isIgnored(relativePath:string):boolean{return relativePath.split('/').some(part=>IGNORED.has(part));}
export function indexRepository(root:string,previous?:RepositoryIndex):RepositoryIndex{const files=discoverFiles(root);const old=new Map((previous?.files||[]).map(file=>[file.path,file]));const current:IndexedFile[]=[];const added:string[]=[];const changed:string[]=[];const reused:string[]=[];for(const filePath of files){const absolute=path.join(root,filePath);const stat=fs.statSync(absolute);const hash=fileHash(absolute);const prior=old.get(filePath);if(prior&&prior.hash===hash){current.push({...prior,size:stat.size,mtimeMs:stat.mtimeMs});reused.push(filePath);}else{current.push(parseFile(root,filePath,stat,hash));(prior?changed:added).push(filePath);}}const currentSet=new Set(files);const removed=(previous?.files||[]).map(file=>file.path).filter(file=>!currentSet.has(file));const addedByHash=new Map(current.filter(file=>added.includes(file.path)).map(file=>[file.hash,file.path]));const renamed:{from:string;to:string}[]=[];for(const from of removed){const to=addedByHash.get(old.get(from)?.hash||'');if(to)renamed.push({from,to});}const relationships=current.flatMap(file=>file.imports.map(request=>({from:file.path,to:resolveImport(file.path,request,files)}))).filter((x):x is {from:string;to:string}=>Boolean(x.to));const manifests=current.filter(file=>MANIFESTS.has(path.basename(file.path))).map(file=>file.path);return{version:1,generatedAt:new Date().toISOString(),root:path.resolve(root),directories:directoriesOf(files),files:current,moduleRelationships:relationships,manifests,dependencies:readDependencies(root,manifests),incremental:{reused,added,changed,removed:removed.filter(file=>!renamed.some(rename=>rename.from===file)),renamed}};}
export function writeRepositoryIndex(root:string,previous?:RepositoryIndex):RepositoryIndex{const index=indexRepository(root,previous);const output=path.join(root,'.lgs');fs.mkdirSync(output,{recursive:true});fs.writeFileSync(path.join(output,'index.json'),JSON.stringify(index,null,2)+'\n');fs.writeFileSync(path.join(output,'CODEBASE_MAP.md'),renderCodebaseMap(index));return index;}
export function renderCodebaseMap(index:RepositoryIndex):string{const tick=String.fromCharCode(96);const byDirectory=new Map<string,IndexedFile[]>();for(const file of index.files){const directory=path.dirname(file.path);const list=byDirectory.get(directory)||[];list.push(file);byDirectory.set(directory,list);}const lines=['# LGS Codebase Map','','Generated: '+index.generatedAt,'','This map is generated deterministically from the filesystem, manifests, and source syntax. Source files remain authoritative.','','## Repository shape','', '- Files: '+index.files.length,'- Directories: '+index.directories.length,'- Manifests: '+(index.manifests.join(', ')||'none'),'','## Directories and important files',''];for(const [directory,files] of [...byDirectory.entries()].sort()){lines.push('### '+directory);for(const file of files.slice(0,40)){const tags=[file.likelyTest?'test':'',file.likelyDocumentation?'documentation':''].filter(Boolean).join(', ');lines.push('- '+tick+file.path+tick+' — '+(file.language||file.fileType)+(tags?' ('+tags+')':''));if(file.symbols.length)lines.push('  - Symbols: '+file.symbols.slice(0,12).join(', '));}lines.push('');}lines.push('## Module relationships','');for(const relation of index.moduleRelationships.slice(0,120))lines.push('- '+tick+relation.from+tick+' → '+tick+relation.to+tick);if(!index.moduleRelationships.length)lines.push('- No local import relationships detected.');lines.push('','## Dependencies','');for(const [name,version] of Object.entries(index.dependencies).sort())lines.push('- '+tick+name+tick+': '+version);if(!Object.keys(index.dependencies).length)lines.push('- No package dependencies detected.');lines.push('','## Incremental update','', '- Reused: '+index.incremental.reused.length,'- Added: '+index.incremental.added.length,'- Changed: '+index.incremental.changed.length,'- Removed: '+index.incremental.removed.length,'- Renamed: '+index.incremental.renamed.length,'','Rebuild with **LGS: Rebuild Repository Index**.');return lines.join('\n')+'\n';}
function parseFile(root:string,filePath:string,stat:fs.Stats,hash:string):IndexedFile{const extension=path.extname(filePath).toLowerCase();const base={path:filePath,fileType:extension?extension.slice(1):'unknown',language:languageFor(extension),size:stat.size,mtimeMs:stat.mtimeMs,hash,imports:[] as string[],exports:[] as string[],symbols:[] as string[],likelyTest:/((^|[._-])(test|spec))|(^|\/)test(s)?\//i.test(filePath),likelyDocumentation:DOCS.has(extension)||/readme|changelog|license/i.test(path.basename(filePath))};if(!SOURCE.has(extension))return base;try{const source=fs.readFileSync(path.join(root,filePath),'utf8');const kind=extension.includes('tsx')?ts.ScriptKind.TSX:extension.includes('jsx')?ts.ScriptKind.JSX:extension.includes('js')?ts.ScriptKind.JS:ts.ScriptKind.TS;const ast=ts.createSourceFile(filePath,source,ts.ScriptTarget.Latest,true,kind);const imports:string[]=[];const exports:string[]=[];const symbols:string[]=[];for(const statement of ast.statements){if(ts.isImportDeclaration(statement)&&ts.isStringLiteral(statement.moduleSpecifier))imports.push(statement.moduleSpecifier.text);if(ts.isExportDeclaration(statement)&&statement.moduleSpecifier&&ts.isStringLiteral(statement.moduleSpecifier))imports.push(statement.moduleSpecifier.text);if(ts.isExportDeclaration(statement)&&statement.exportClause&&ts.isNamedExports(statement.exportClause))statement.exportClause.elements.forEach(element=>exports.push(element.name.text));if(ts.isExportAssignment(statement))exports.push('default');const name=declarationName(statement);if(name)symbols.push(name);if(name&&ts.canHaveModifiers(statement) && ts.getModifiers(statement)?.some(modifier=>modifier.kind===ts.SyntaxKind.ExportKeyword))exports.push(name);}const requirePattern=/\brequire\s*\\(\s*[']([^'"]+)['"]\s*\\)/g;let match:RegExpExecArray|null;while((match=requirePattern.exec(source)))imports.push(match[1]);return{...base,imports:[...new Set(imports)],exports:[...new Set(exports)],symbols:[...new Set(symbols)]};}catch{return base;}}
function declarationName(node:ts.Node):string|undefined{if((ts.isFunctionDeclaration(node)||ts.isClassDeclaration(node)||ts.isInterfaceDeclaration(node)||ts.isTypeAliasDeclaration(node)||ts.isEnumDeclaration(node)||ts.isModuleDeclaration(node))&&node.name)return node.name.text;if(ts.isVariableStatement(node))return node.declarationList.declarations.map(declaration=>ts.isIdentifier(declaration.name)?declaration.name.text:'').filter(Boolean).join(', ')||undefined;return undefined;}
function resolveImport(from:string,request:string,files:string[]):string|undefined{if(!request.startsWith('.'))return undefined;const base=path.posix.normalize(path.posix.join(path.posix.dirname(from),request));const withoutExtension=SOURCE.has(path.posix.extname(base))?base.slice(0,-path.posix.extname(base).length):base;const candidates=[base,withoutExtension,...[...SOURCE].map(extension=>withoutExtension+extension),...[...SOURCE].map(extension=>withoutExtension+'/index'+extension)];return candidates.find(candidate=>files.includes(candidate));}
function readDependencies(root:string,manifests:string[]):Record<string,string>{const result:Record<string,string>={};for(const manifest of manifests.filter(file=>path.basename(file)==='package.json')){try{const json=JSON.parse(fs.readFileSync(path.join(root,manifest),'utf8')) as Record<string,unknown>;for(const section of ['dependencies','devDependencies','peerDependencies','optionalDependencies']){const values=json[section];if(typeof values==='object'&&values!==null)for(const [name,version] of Object.entries(values))if(typeof version==='string')result[name]=version;}}catch{// Ignore malformed manifests.
}}return result;}
function fileHash(file:string):string{return createHash('sha1').update(fs.readFileSync(file)).digest('hex');}function relative(root:string,file:string):string{return path.relative(root,file).split(path.sep).join('/');}function directoriesOf(files:string[]):string[]{return[...new Set(files.flatMap(file=>{const parts=file.split('/');return parts.slice(0,-1).map((_,index)=>parts.slice(0,index+1).join('/'));}))].sort();}function languageFor(extension:string):string{if(['.ts','.tsx'].includes(extension))return'TypeScript';if(['.js','.jsx','.mjs','.cjs'].includes(extension))return'JavaScript';if(extension==='.json')return'JSON';if(DOCS.has(extension))return'Markdown/Text';if(extension==='.css')return'CSS';if(extension==='.svg')return'SVG';return'';}
