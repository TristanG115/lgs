import type { BackendConfig, ModelBackend } from './backend.js'; import { AnthropicBackend } from './anthropic.js'; import { OllamaBackend } from './ollama.js'; import { OpenAICompatibleBackend } from './openai.js';
export type ProviderKind='ollama'|'openai-compatible'|'anthropic'; export type BackendProfile={id:string;name:string;kind:ProviderKind;baseUrl:string;secretName?:string};
export const defaultProfiles=():BackendProfile[]=>[{id:'ollama-local',name:'Ollama (local)',kind:'ollama',baseUrl:'http://localhost:11434'},{id:'openai',name:'OpenAI',kind:'openai-compatible',baseUrl:'https://api.openai.com/v1',secretName:'lgs.secret.openai'},{id:'openai-local',name:'OpenAI Compatible (local)',kind:'openai-compatible',baseUrl:'http://localhost:1234/v1'},{id:'anthropic',name:'Anthropic',kind:'anthropic',baseUrl:'https://api.anthropic.com/v1',secretName:'lgs.secret.anthropic'}];
export const createBackend=(profile:BackendProfile,secret?:string):ModelBackend=>{const c:BackendConfig={id:profile.id,displayName:profile.name,baseUrl:profile.baseUrl,secretKey:secret};if(profile.kind==='ollama')return new OllamaBackend(c);if(profile.kind==='anthropic')return new AnthropicBackend(c);return new OpenAICompatibleBackend(c);};
