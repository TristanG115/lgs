import { ToolRegistry } from '../tools/framework.js';
import type { ToolPermission } from '../tools/types.js';
import type { VerifiedCommitService } from './service.js';
const PERMISSION: ToolPermission = { access: 'execute', scope: 'workspace', network: false, category: 'git-mutation' };
export function registerCommitTools(registry: ToolRegistry, service: VerifiedCommitService): ToolRegistry { registry.register({ id: 'create_verified_commit', description: 'Create a detailed commit only after all Completion Guard gates pass. Stages only task-attributable files and blocks user-change overlap or obvious secrets.', permission: PERMISSION, argumentSchema: { type: 'object', properties: { type: { type: 'string', minLength: 1, maxLength: 30 }, scope: { type: 'string', minLength: 1, maxLength: 50 }, summary: { type: 'string', minLength: 1, maxLength: 120 }, changes: { type: 'string', minLength: 1, maxLength: 4000 }, documentation: { type: 'string', minLength: 1, maxLength: 2000 }, verification: { type: 'string', minLength: 1, maxLength: 4000 } }, required: ['type', 'scope', 'summary', 'changes', 'documentation', 'verification'], additionalProperties: false }, execute: async (args, context) => { if (!context.taskId) throw new Error('Verified commits require an LGS task ID.'); return { data: await service.commit(context.taskId, args as never), resultCount: 1, source: 'git' }; } }); return registry; }
